<?xml version="1.0" encoding="utf-8"?>
<!-- Only the one hand-off directory CaptureExport writes bordered copies into.
     Nothing else in the app's storage is exposed to other apps. -->
<paths>
    <cache-path
        name="shares"
        path="shared/" />
</paths>
